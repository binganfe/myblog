spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=US/Eastern
    username: root
    password: root
  thymeleaf:
    mode: HTML
    cache: false
  profiles:
    active: dev

comment.avatar: /images/avatar.png